name: dispatch event

on: 
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: dispatch event
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INSIGHTS_LINUX_DISPATCH_PAT }}
          repository: genians/insights-linux
          event-type: submodule_updated
          client-payload: >
            {
              "repository": "insights-watcher", 
              "pr_title": "test update water",
              "pr_number": "${{ github.event.pull_request.number }}"
            }
